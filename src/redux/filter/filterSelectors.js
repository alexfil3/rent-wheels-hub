export const selectFilteredCars = state => state.filter.items;

export const selectFormData = state => state.filter.formData;

export const selectFindItems = state => state.filter.foundItems;

export const selectIsOpenBrand = state => state.filter.isOpenBrand;

export const selectIsOpenPrice = state => state.filter.isOpenPrice;
